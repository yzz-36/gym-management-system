<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.CardApplicationMapper">

    <select id="findAll" resultType="cardApplication">
        SELECT *
        FROM card_application
        ORDER BY id DESC
    </select>

    <select id="findByMemberAccount" resultType="cardApplication" parameterType="int">
        SELECT *
        FROM card_application
        WHERE member_account = #{memberAccount}
        ORDER BY id DESC
    </select>

    <insert id="insert" parameterType="cardApplication">
        INSERT INTO card_application (member_account, member_name, member_phone, apply_time, status, remark)
        VALUES (#{memberAccount}, #{memberName}, #{memberPhone}, #{applyTime}, #{status}, #{remark})
    </insert>

    <update id="updateStatus" parameterType="cardApplication">
        UPDATE card_application
        SET status = #{status},
            remark = #{remark}
        WHERE id = #{id}
    </update>

</mapper>
